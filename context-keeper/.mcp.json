{
  "mcpServers": {
    "carry-forward": {
      "type": "stdio",
      "command": "uvx",
      "args": [
        "--from", "fastmcp",
        "fastmcp", "run",
        "${CLAUDE_PLUGIN_ROOT}/src/server.py"
      ]
    }
  }
}
