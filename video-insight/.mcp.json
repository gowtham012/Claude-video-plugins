{
  "mcpServers": {
    "video-insight": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "run",
        "--project", "${CLAUDE_PLUGIN_ROOT}",
        "video-insight-server"
      ]
    }
  }
}
